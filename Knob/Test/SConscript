import os

Import('env')

# uic knobtest form
env.Uic4('KnobTest.ui')

# build knobtest
knobtest = env.Program(
   'knobtest',
   'main.cpp',
   LIBS=['QtCore','QtGui','knob','qwt'],
   LIBPATH=['../','$QWTDIR/lib','$QTDIR/lib']
)

Default(knobtest)

env.InstallBin(knobtest)